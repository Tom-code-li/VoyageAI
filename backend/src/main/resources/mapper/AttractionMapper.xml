<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.travelassistant.mapper.AttractionMapper">

    <select id="selectByCity" resultType="com.example.travelassistant.entity.Attraction">
        SELECT
            id,
            city,
            name,
            description,
            image_url,
            reference_count,
            play_time,
            longitude,
            latitude,
            create_time,
            update_time
        FROM attraction
        WHERE city = #{city}
        ORDER BY reference_count DESC, update_time DESC, name ASC
    </select>

    <select id="selectCityDistribution" resultType="com.example.travelassistant.dto.ChartItemVO">
        SELECT
            city AS name,
            COUNT(*) AS value
        FROM attraction
        WHERE city IS NOT NULL AND city != ''
        GROUP BY city
        ORDER BY value DESC, city ASC
    </select>
</mapper>
